# Use of config.py → Stores configuration settings like database URL, server port, model paths, and constants.

MONGO_URI = "mongodb://localhost:27017"
DB_NAME = "honeypotx"

SERVER_HOST = "0.0.0.0"
SERVER_PORT = 8000

MODEL_PATH = "ai_engine/model.pkl"